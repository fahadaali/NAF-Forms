/**
 * قيم hex مشتقّة من رموز `naf-theme` — للمواضع التي **لا تقبل متغيّر CSS**.
 *
 * القاعدة ١ تمنع كتابة قيمة تصميم حرفية، والمخرج الطبيعي لذلك هو
 * `var(--primary)`. لكن أربعة سياقات في هذه المنصة لا تُحلّ فيها متغيّرات CSS:
 *
 *   ١) قيم تُخزَّن في D1 كنصّ (لون المشروع، وثيم النموذج الذي يختاره صاحبه)
 *   ٢) `<input type="color">` — لا يقبل إلا hex صريحًا
 *   ٣) `canvas` — لا يقرأ أصناف CSS ولا متغيّراتها
 *   ٤) SVG داخل `data:` URI — معزول عن ورقة أنماط الصفحة
 *
 * فبدل نثر قيم حرفية في تسعة ملفات، جُمعت هنا في مصدر واحد، وكل قيمة
 * موسومة بالرمز المشتقّة منه حتى تُحدَّث معه عند تغيّر الثيم.
 *
 * **التحويل من oklch إلى sRGB يدوي ومحسوب مرة واحدة.** عند رفع تثبيت
 * `naf-theme` يجب إعادة حسابه — لا تُعدَّل هذه القيم بالتخمين.
 *
 * القيم أدناه من `naf-theme#v1.18.0`، الوضع الفاتح. الوضع الداكن غير مذكور
 * عمدًا: هذه القيم تُخزَّن أو تُرسم مرة واحدة ولا تنقلب مع الوضع.
 */

/** `--primary` · oklch(0.5700 0.0904 80.9413) */
export const NAF_PRIMARY = "#937133";

/** `--background` · oklch(0.9383 0.0042 236.4993) */
export const NAF_BACKGROUND = "#e8ebed";

/**
 * `--background` في الوضع الداكن · oklch(0.2598 0.0306 262.6666)
 *
 * لموضع واحد: `<meta name="theme-color">` في `app/layout.tsx` — الوسم
 * يأخذ لونًا حرفيًا ولا يحلّ `var()` (الاستثناء الرابع في القاعدة ١).
 * ولا يُستهلك في واجهة: الواجهة تأخذ `bg-background` فينقلب معها.
 */
export const NAF_BACKGROUND_DARK = "#1c2433";

/** `--foreground` · oklch(0.3211 0 0) */
export const NAF_FOREGROUND = "#333333";

/** `--card` · oklch(1.0000 0 0) */
export const NAF_CARD = "#ffffff";

/** `--muted-foreground` · oklch(0.5250 0.0234 264.3637) */
export const NAF_MUTED_FOREGROUND = "#646a78";
