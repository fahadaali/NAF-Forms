# سجلّ التغييرات

يُولَّد آلياً من أوصاف الالتزامات. لا يُحرَّر يدوياً.

## 1.1.1 — 2026-07-29

### إصلاحات

- الترويسة تعرض الاسم لا البريد — يُقرأ من صفّ العضو في المركز (`bbb6e48`)

## 1.1.0 — 2026-07-29

### ميزات

- الاستبانات تأخذ هيكل ناف الموحّد (`3ead52f`)

### إصلاحات

- مزامنة ملف الهيكل مع السجلّ — سطرا الحساب والحشو (`16b7a0b`)

## 1.0.4 — 2026-07-29

### إصلاحات

- رفعُ تثبيت naf-auth إلى v3.3.0 (`fc31cce`)

## 1.0.3 — 2026-07-28

### إصلاحات

- مسار إشعار الخروج الخلفي — الخروج من المركز يُنهي الجلسة هنا (`25dcca7`)

## 1.0.2 — 2026-07-28

### إصلاحات

- ترقية naf-auth إلى v3.1.2، ومركز الفحص يحاكي القراءة ثم الكتابة (`7489bab`)

## 1.0.1 — 2026-07-28

### إصلاحات

- ترقية naf-auth إلى v3.1.1، ومحاكاة المركز توافق عقده الجديد (`74f63a1`)

## 1.0.0 — 2026-07-28

### تغييرات كاسرة

- «مستخدم (اطّلاع)» يقرأ ولا يكتب (`364f5a5`)

### إصلاحات

- ملف القفل يشير إلى v3.1.0 كما يشير الوصف (`be7ecfe`)
- تعذّر تبليغ المنح يأخذ نصّه، لا نصّ فشل الاتصال (`4733929`)
- الخروج يخرج بالمستخدم، والمركز يعرض صلاحيته في هذه المنصة (`32fd9c5`)

## 0.1.0 — 2026-07-28

### ميزات

- ربط المنصة بالدخول الموحّد عبر naf-auth (`136a297`)
- تبنّي مكوّنَي التنبيه والجدول من السجلّ (`491eda4`)
- تبنّي مكوّن البطاقة من السجلّ وحذف صنف card المحلي (`5d2bf46`)
- تبنّي مكوّن الحقل من السجلّ وحذف صنف input المحلي (`485176b`)
- تبنّي مكوّن الزرّ من السجلّ وحذف أصناف الأزرار المحلية (`5ff0313`)
- تثبيت شعار ناف ومكتبة cn من السجلّ، وإزالة ما بقي من القيم الحرفية (`81bd909`)
- استبدال مجموعة الأيقونات المحلية بـ Lucide من السجلّ (`8b3c365`)

### إصلاحات

- إغلاق طريق الدخول الثاني، والمستثنى من الوسيط أصولٌ مسمّاة (`dd61391`)
- معالجة ٤٠١ و٤٠٣ في عميل اللوحة (`871e1a7`)
- شكل الردّ يتبع طبيعة الطلب، وتسجيل حالة ردّ المركز (`f5813eb`)
- تثبيت naf-auth على v3.0.0 وقيمتا المنصة (`3c69b76`)
- اعتماد «محرّر» المنشورة في السجلّ لدور editor (`1bd719a`)
- إزالة النصّ الوصفي المسكوك من شاشة الصلاحيات (`7024270`)
- معالجة انحرافات فحص الهوية — الحالات والنصوص (`0f39fb4`)
- مصدر واحد لقيم الهوية الحرفية، وإتمام التنسيق من naf-format (`f6d222f`)
- مراجعة الأيقونات على أساس المعنى والشكل لا الاسم (`7df79ed`)
- معالجة ما كشفته قائمة تعريف الإنجاز (`05f9db6`)
